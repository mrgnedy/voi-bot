import 'package:flutter/material.dart';
import 'myApp.dart';
import './MainPage.dart';

void main() => runApp(new MyApp());
final MainPage drawerPage = MainPage();
class ExampleApplication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MainPage();
  }
}
